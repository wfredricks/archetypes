# nl-chat-substrate — Reference Implementation (pre-lift pointer)

**Canonical reference: the SIG/MCP natural-language pipeline.**

Likely lineage:

- [`wfredricks/sig-mcp`](https://github.com/wfredricks/sig-mcp) (MCP tool surface)
- `polygraph-viz/src/nl/` (intent extraction, slot filling, routing)

Exact repo lineage to be confirmed at lift time — when the **second derivation**
trigger condition (see `../ARCHETYPE.md`) is met.

This archetype is a **stub** in this registry. When the second adoption happens,
this pointer will be joined by a full `ARCHETYPE.md`, `ADOPTION-RECIPE.md`,
`DEFECTS.md`, and `ADOPTIONS.md`.

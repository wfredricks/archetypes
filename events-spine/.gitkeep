# events-spine — placeholder directory
#
# The events-spine composite archetype is built in step (c) of the 2026-05-21
# build series (the step that follows this registry's bootstrap). It will be
# the first **in-tree** reference implementation in this registry.
#
# Planned composition:
#   - simple-pubsub
#   - simple-subscriber
#   - scribe
#   - mcp-proxy
#
# See COMPOSITION.md for the events-spine YAML example (forward-looking).
#
# When step (c) lands, this .gitkeep is replaced by the full archetype document
# set plus reference-impl/ code.

# <archetype-name> — Known Defects

*Every defect discovered during an adoption is documented here. The next adopter
inherits this list and either fixes what applies or documents knowing acceptance.*

> **TODO:** keep this in sync with `ARCHETYPE.yaml#defects_known`. The YAML carries
> short summaries; this file carries full context and remediation status.

---

## Format

For each defect:

```markdown
### <short-name>

- **Discovered:** <date>, <adopter project / stage>
- **Upstream status:** <flagged | patched | won't-fix | unknown>
- **Local fix:** <how the discovering adopter fixed it (or noted knowing acceptance)>
- **Recommendation for new adopters:** <what to do>
- **Detail:** <full context — symptoms, root cause, why it matters>
```

## Defects

> **TODO:** initial list. Empty if no adoptions yet — the first adopter will start
> filling this in via the recipe-file build findings (see
> `../METHODOLOGY.md` §"Recipe-file builds" and §"Defects-fix discipline").

*(none yet)*

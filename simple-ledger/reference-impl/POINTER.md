# simple-ledger — Reference Implementation (pre-lift pointer)

**Canonical reference: [`wfredricks/chainblocks`](https://github.com/wfredricks/chainblocks)**

This archetype is a **stub** in this registry — see the directory's `ARCHETYPE.md`
for the trigger condition that will produce the full lift. The library, however,
already exists at the URL above and is consumed indirectly by `solution-intelligence-identity`
via its `src/audit.ts` chainblocks wrapper.

When Stage 3 (the chainblocks → SI/G adoption) ships, this pointer will be
joined by a full `ARCHETYPE.md`, `ADOPTION-RECIPE.md`, `DEFECTS.md`, and
`ADOPTIONS.md` — all populated from the SI/G recipe-file build artifacts the same
way `simple-auth/` was populated from SI/I.

Until then, treat the chainblocks repo itself as the source of truth for the
pattern.
